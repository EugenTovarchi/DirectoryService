apiVersion: 1

datasources:
  # Общий Loki datasource для backend-сервисов и будущего frontend.
  - name: Loki
    uid: loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: true
